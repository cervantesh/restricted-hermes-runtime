# Corrected A0 candidate publication closure

## Scope

This record binds the corrected P2 candidate `7021786` to its immutable tag,
published Linux/amd64 subjects, exact workflow run, and a Windows replay using
the UTF-8 fail-closed verifier. It retains only content-safe identifiers and
artifact hashes; full generated artifacts remain in the workflow retention
boundary.

## Verified facts

- Tag `immutable-candidate-2026-09-12-7021786` resolves to `7021786`.
- Workflow run `34703552764` built, attested, SBOM-verified, consumed, and
  exercised both digest-pinned subjects successfully.
- A detached checkout of that tag validated the downloaded manifest using the
  verifier at `59e2bcc`, including external attestation checks.

## Nonclaims

This is synthetic, bounded candidate evidence. It does not establish A0/A1,
representative-host conformance, PHI authorization, HIPAA/BAA status, clinical
readiness, or production deployment conformance.
