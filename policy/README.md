The deployment owner substitutes exact approved identity/project values, signs
the RFC 8785 canonical bytes offline with an Ed25519 key, and provides only
the public key and detached base64 signature to the gateway. This template is
not a usable policy and is intentionally not signed.
