FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml ./
COPY src ./src
COPY migrations ./migrations
RUN pip install --no-cache-dir . && useradd --system --uid 10003 restricted-migration
USER 10003
CMD ["python", "-m", "restricted_runtime.migration_runner"]
